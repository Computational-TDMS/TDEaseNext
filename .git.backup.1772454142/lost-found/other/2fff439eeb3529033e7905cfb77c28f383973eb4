# OpenSpec 变更提案：新增 OpenMS PeakPicker 节点

## 背景与动机

- 现有工作流已支持 TopFD/TopPIC 等核心工具，但缺少 OpenMS 的基础峰提取能力。
- 引入 `PeakPickerHiRes` 可在早期阶段进行高分辨率峰检测，改进后续特征识别与定量。

## 目标与范围

- 在工具注册表中新增 `peak_picker`（OpenMS `PeakPickerHiRes`）。
- 支持从 `mzML` 输入到 `mzML` 输出的标准化端口与文件模式。
- 在转译器（V2/V3）生成规则时，正确映射参数与 Conda 环境。

## 设计方案

### 工具注册表条目（JSON 示例）

```json
{
  "peak_picker": {
    "tool_type": "command",
    "tool_path": "PeakPickerHiRes",
    "input_params": ["input_files"],
    "output_patterns": [
      { "pattern": "{input_basename}.mzML", "handle": "mzml" }
    ],
    "param_mapping": {
      "mass_tolerance": { "flag": "-mass_tolerance", "type": "value" },
      "intensity_threshold": { "flag": "-intensity_threshold", "type": "value" },
      "threads": { "flag": "-threads", "type": "value" }
    },
    "output_flag_supported": true,
    "conda_env": "envs/openms.yaml",
    "execution_mode": "native",
    "description": "OpenMS 高分辨率峰检测"
  }
}
```

说明：
- `tool_path` 使用 OpenMS 提供的可执行名；通过 `app/core/platform_manager.py` 自动发现。
- `output_patterns` 采用与输入同基名的 `mzML` 输出；可根据配置覆盖输出路径。
- `conda_env` 指向项目根的 `envs/openms.yaml`，由规则引用。

### 端口与连接约定

- 输入端口：`input_files`（接受 `mzML` 文件列表或单个文件）。
- 输出句柄：`mzml`（下游节点以 `targetHandle: "input_files"` 接收）。

### 参数与命令映射

- 支持常用参数：`mass_tolerance`、`intensity_threshold`、`threads`。
- 命令行组装遵循现有逻辑：
  - 选项参数按 `param_mapping` 转换（参考 `src/nodes/tool_node.py`）。
  - 输入文件作为位置参数或通过 `-in/-out` 风格（OpenMS 支持 `-in`/`-out`）。

示例命令（最终由规则生成）：

```
PeakPickerHiRes -in {input.mzml} -out {output.mzml} -mass_tolerance {params.mass_tolerance} -threads {params.threads}
```

### 转译与规则生成

- V3 路径：VueFlow JSON → `translator_v3` → `config.yaml` + `Snakefile`。
- 在生成规则时：
  - `input`：来自上游 `mzML` 输出句柄。
  - `output`：`results/peak_picker/{sample}.mzML`（或 `output_patterns` 指定形式）。
  - `params`：从节点 `params` 平铺映射。
  - `conda`：`envs/openms.yaml`。

### Conda 环境

- 在项目根添加或复用 `envs/openms.yaml`，内容参考现有文档：

```yaml
channels:
  - conda-forge
  - bioconda
dependencies:
  - openms=3.0.0
  - python=3.9
```

## 验收标准

- 在 `tests/` 中新增用例，验证：
  - VueFlow JSON 含 `peak_picker` 节点能生成可编译的 `Snakefile`。
  - `snakemake --dry-run` 通过（`app/services/runner.py:152` 统一调用）。
  - 输出模式和端口连接正确（下游节点可消费 `mzml`）。

## 兼容性与迁移

- 不影响现有 TopFD/TopPIC 流程；作为独立节点插入。
- 若已有自定义输出目录策略，保留 `output_flag_supported: true` 与 `-out` 路径。

## 风险与缓解

- 平台差异：可执行发现失败 → 走 `platform_manager` 与 Conda 环境保证一致性（`app/core/platform_manager.py:30`）。
- 参数不兼容：通过注册表 `param_mapping` 显式定义，避免默认 `--param` 导致错误。

## 执行计划

1. 注册表新增 `peak_picker` 条目（`config/tools/` 或内置 `_load_default_registry`）。
2. 在转译器中加入输出句柄映射与规则模板（`src/snakemake/translator_v3.py`）。
3. 添加测试用例与示例工作流。
4. 文档更新 `openspec/project.md` 与技术栈说明。

